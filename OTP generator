import math, random
def generateOTP():
  digits = "0123456789"
  OTP = ""
  print("OTP Generation")
  for i in range(6):
        OTP += digits[math.floor(random.random() * 10)]
  return OTP

if _name_ == "_main_":
  print("OTP of 6 digits:", generateOTP())
